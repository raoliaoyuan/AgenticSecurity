import React from 'react'
import ReactDOM from 'react-dom/client'
import ArchitectureViz from './arch'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <ArchitectureViz />
    </React.StrictMode>,
)
